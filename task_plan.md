# 插件基础安全与端口路由优化计划

## 目标

在不引入依赖、不修改 Tauri 安全配置、不过度扩展架构的前提下，完成第一批可回滚优化：修复插件节点多输出端口路由，并阻止插件用任意远程媒体 URL 绕过宿主能力边界。

## 范围

- `src/services/plugins/pluginRuntime.ts`
- `tests/services/pluginRuntime.test.ts`
- `doc/插件开发规范.md`

本计划、发现与进度文件仅用于记录本次实施，不属于产品运行代码。

## 阶段

1. [completed] 核对工作区、编码、真实调用链和测试基线
2. [completed] 实现按 `sourceHandle` 精确取值及端口类型校验
3. [completed] 实现远程媒体引用来源校验
4. [completed] 补充回归测试与开发规范
5. [completed] 运行定向测试、类型、Lint、UTF-8 与差异检查
6. [completed] 审阅最终差异并形成移交说明

## 验收标准

- 插件节点连接多个输出时，目标输入读取所连端口的精确值。
- 已知的插件端口类型不兼容时，运行时给出明确错误。
- 插件不能凭空返回会被宿主加载的 `http/https` 媒体地址。
- 来自本次输入或宿主本轮模型调用结果的远程媒体地址可以继续传递。
- 定向插件测试、TypeScript 类型检查、目标文件 ESLint、UTF-8 和 `git diff --check` 通过，或明确记录既有阻断。

## 风险与回滚

- 风险：旧插件若曾直接返回任意外链媒体，将被明确拒绝；文档需同步说明迁移方式。
- 兼容边界：远程媒体来源校验仅约束 QuickJS JavaScript；可信 Python 本机代码的既有行为不变。
- 风险：旧连线缺少句柄元数据时必须保留兼容回退。
- 回滚：仅撤销上述三份产品文件的本批差异；不触碰工作区内其他现有改动。

## 错误记录

| 时间 | 命令/步骤 | 结果 | 处理 |
|---|---|---|---|
| 2026-08-30 | 初始状态检查 | 工作区已有大量无关改动；三份目标产品文件均未改动 | 使用显式目标文件清单，禁止覆盖无关改动 |
| 2026-08-30 | Windows 下 `rg ... src/types/*.ts` | PowerShell/Windows 将该通配路径交给 `rg` 后报路径语法错误 | 改用 `rg -g '*.ts' ... src/types`，检索成功 |
| 2026-08-30 | 第二次 `npm run typecheck` | 被无关并行改动 `ComposerLayerNode.tsx` 的 `resourceBudgetError` 类型错误阻断 | 本批代码落下后的第一次 typecheck 已通过；保留阻断证据，不修改无关文件，最终再复查一次 |
| 2026-08-30 | 第一组暂存清单自动比对 | Git 默认把中文路径显示为八进制转义，造成 1 个 unexpected / 1 个 missing 假告警 | 改用 `git -c core.quotepath=false` 复核真实路径；暂存数量仍为预期 14 个 |
| 2026-08-30 | 文档审计中 `rg src/services/plugins/*.ts` | Windows 将星号路径交给 `rg` 后报路径语法错误 | 已改用目录参数配合 `-g '*.ts'`；不重复使用该通配写法 |
| 2026-08-30 | 文档旧语义扫描首次使用 lookahead | `rg` 默认正则不支持 look-around | 改用 `--pcre2` 后完成扫描 |
| 2026-08-30 | 严格 UTF-8 脚本首次拼接 `$file:` | PowerShell 把变量名与冒号一起解析并报错 | 改用 `${file}`，五份文档均通过 |
| 2026-08-30 | 终检旧语义规则包含宽泛“排除列表” | 命中“**不按**模型开销维护隐藏排除列表”的正确否定文案，属于假阳性 | 收窄为旧句原文后复查，无旧语义命中 |

## 完成结论

- 三份产品文件内的第一批插件优化已完成。
- 最终全仓类型检查恢复通过；中途阻断来自并行中的无关文件，未越界处理。
- 最终安全复审确认 Markdown 图片、画布笔记 CSS 颜色和缺失来源插件三项阻断均已解除。

## 后续审计：未提交改动归档

用户追加要求：审计当前全部未提交文件，仅将完整、有用、验证通过且边界清晰的改动提交；保持无关或未完成改动不动。

1. [completed] 盘点全部差异并按功能来源分组
2. [completed] 审阅各组调用链、完整性、安全边界与文档一致性
3. [completed] 对候选组运行定向检查并确认 UTF-8、差异与工作区边界
4. [completed] 使用显式文件清单分组提交，复核提交内容与剩余改动

### 本轮提交边界

- 不使用 `git add .`，不把过程记录自动混入产品提交。
- 不修补与候选组无关的代码；发现不完整或高风险改动时保留在工作区并报告。
- 若改动属于架构收敛、新增依赖、删除文件或安全配置变更，先按项目检查点暂停确认。

### 当前检查点

- 插件原生信任注册表属于架构收敛，已完成只读审阅与验证，等待用户确认文件范围、影响与回滚后提交。
- 候选提交不新增依赖、不删除文件、不修改 `tauri.conf.json`，但会新增原生命令 ACL 和 Rust 私有执行边界。

### 提交结果

- `30ddc43 feat(plugin): 建立原生插件信任注册与安全执行边界`
- `5f4b0d0 feat(director): 保存 Blender 编辑结果并回传当前镜头`
- `561ad84 chore(git): 忽略本机 Cargo 与规划目录`

## 后续文档同步：插件信任注册与 Blender 1.3.2

用户要求更新以下 5 份长期文档，使其与提交 `30ddc43`、`5f4b0d0`、`561ad84` 及当前代码一致：

- `README.md`
- `AGENTS.md`
- `doc/开发指南.md`
- `doc/插件开发规范.md`
- `doc/架构说明.md`

1. [completed] 并行审计五份文档现状、重复信息与过期描述
2. [completed] 对照真实代码、ADR 与版本清单形成逐文件修改摘要
3. [completed] 按 5 文件检查点向用户确认范围、风险、验收与回滚
4. [completed] 仅修改确认的五份文档并保持 UTF-8
5. [completed] 校验链接、关键标识、乱码与 `git diff --check`

6. [completed] 用显式五文件清单提交文档更新
7. [completed] 推送 `master` 到 `myfork/master`
8. [completed] 核对上游差异并创建或更新“今天全部改动”PR

### 文档同步边界

- 不修改产品代码、版本号、依赖、安全配置或生成 ACL。
- README 只做面向用户的能力概览，不堆实现细节；工程约束放 `AGENTS.md`，开发流程放开发指南，插件契约放插件规范，模块关系放架构说明。
- 不把本机绝对路径、API Key、完整源码或未经验证的运行结论写入文档。
- 回滚仅撤销上述五份文档的本轮差异。

### 待确认的逐文件摘要

- `README.md`：新增用户插件能力与规范入口；纠正 MCP 为自主模式不逐次审批；把导演台改为轻量网页 / 本机 Blender 双运行时，并说明保存 `.blend` 后回传当前镜头；补可选 Blender 环境要求。
- `AGENTS.md`：补插件与 Blender 模块目录；把 Store/IndexedDB 漂移数字改为当前真值及源码权威；新增 `sourceDigest` 租约、Rust 私有注册表、不透明执行身份、Python 取消和 `open-editor` 双产物长期规则。
- `doc/开发指南.md`：版本更新到 0.9.0；补 Web 模式插件限制、插件安装执行链、正确 MCP 自主权限、目录结构、Blender 1.3.2 调试与内嵌资源升级流程。
- `doc/插件开发规范.md`：澄清 SHA-256 不是签名/审计；调整启停/previous/卸载语义；补旧记录迁移与失败关闭；链接原生信任注册表完整文件名。
- `doc/架构说明.md`：版本更新到 0.9.0；补插件服务层/注册表/私有存储/数据流；同步 21 slices、IndexedDB v20 与 MCP 最大权限；补 Blender 双产物、私有资源路径和模块树。

### 验收与风险

- 验收：只出现上述五份已跟踪文档差异；严格 UTF-8；本地 Markdown 链接可解析；关键版本/模块/策略与源码一致；`git diff --check` 通过。
- 风险：中文 README 更新后，英/日/韩 README 暂时不同步；本轮按用户清单不扩大到翻译文件。MCP 最大权限和可信 Python 本机权限必须直写，不能用“安全沙箱”或“仍会审批”等含糊文案弱化风险。

### Git 与 PR 边界

- 只提交五份目标文档，不提交 `task_plan.md`、`findings.md`、`progress.md`。
- 推送目标为用户 fork 的 `myfork/master`；上游 `origin/master` 只通过 PR 请求合并，不直接 push。
- 创建 PR 前先刷新两个远端、核对提交范围、检查是否已有同 head/base 的开放 PR；不得创建重复 PR。

## 2026-09-03 实施：插件关联资源授权与主窗口内 UI

### 目标

在不暴露真实路径、项目根目录或插件私有快照的前提下，让节点工具和插件节点按声明读取当前节点自身资源、直接输入连线资源及当前插件版本声明的包资源；将插件 UI 的默认交互收敛为主窗口内弹窗，并为自定义 UI 建立独立、可撤销的会话边界。

### 已确认实施边界

- 不保留尚未对外使用的旧插件 API 兼容层；以统一资源句柄契约迁移仓库内示例、测试和文档。
- 当前节点与直接入边资源默认只读；输出创建新项目资产，原位覆盖不在首批范围。
- 插件只收到 opaque resource id 和必要元数据，不收到绝对路径或原始 `asset://` 本地 URL。
- 包资源由 Manifest 显式声明、纳入完整 revision 摘要并保持只读；可写数据目录与包快照分离。
- 主窗口 `ModalOverlay` 是统一外壳；宿主声明式 UI 优先。第三方自定义代码不得直接挂载到主 React DOM。
- 不新增 npm/cargo 依赖；用户已确认删除 6 个退出调用链的旧 UI/grant 文件。若确需修改 `tauri.conf.json` 或启用 Tauri `unstable`，仍先单独记录风险并停在该检查点。

### 阶段

1. [completed] 恢复上下文，核对工作区、编码、现有 Manifest/runtime/UI/Rust 注册链与测试基线
2. [completed] 收敛首版 v1 Manifest、资源引用类型与权限语义
3. [completed] 实现运行级资源解析与租约：node-self、直接入边、包资源、撤销/复核
4. [completed] 将资源 sidecar 接入 QuickJS/Python 执行输入并保护本地媒体预览引用
5. [completed] 将插件工具 UI 收敛为主窗口 Modal，修复无必填字段时不打开自定义 UI 的分支
6. [completed] 建立隔离自定义 UI 会话与专用 Broker；Windows/非 Windows 专用协议、Tauri 全局注入失败关闭和精确 CSP 白名单均已完成
7. [completed] 补 Manifest、图连接、资源租约、UI 会话与安全回归测试
8. [completed] 更新 `doc/对话助手-Agent能力实施方案.md` 与插件开发规范，完成静态/自动化验证并审阅差异；真实 Tauri 手测按用户要求跳过

### 验收标准

- 节点工具只能读取目标节点自身资源和声明端口的一跳入边资源；插件节点按输入端口获得精确资源集合。
- 出边、二跳、未连接节点、其他项目、其他插件或其他 revision 资源均拒绝。
- 断边、删节点、切项目、结束调用/关 UI、插件停用/升级/卸载会撤销租约。
- 插件包只允许读取当前 revision 清单内资源，路径穿越、符号链接、摘要或大小不符均失败关闭。
- 主窗口只出现一个宿主 Modal；焦点、Esc、主题、错误与提交状态由宿主管理，自定义 UI 无主 DOM、Store、Tauri 通用 API 或任意网络权限。
- 非 v1 Manifest/API 不获得资源权限；持久化数据不能伪装成有效的调用级 grant。
- 定向 Vitest、TypeScript、目标 ESLint、Rust 定向测试/检查、UTF-8 与 `git diff --check` 通过，或明确记录环境阻断。

### 风险与回滚

- 风险：资源引用涉及前端 Store、插件执行输入、原生私有快照与 UI 会话，必须保持完整 revision 绑定并避免异步过期写回。
- 风险：本地 Tauri asset URL 可能编码真实路径，不能继续直接透传给插件。
- 风险：主窗口 iframe 是能力隔离而非独立 CPU/内存隔离；自定义 UI 的死循环或过重渲染仍可能影响主窗口响应。
- 风险：全局 CSP 已按用户确认加入应用自有 `plugin-ui:` 脚本源；协议处理器仍必须保持主 webview、活动插件和精确 UI 摘要三重限制。
- 回滚：资源契约与 revision 身份整体回退；UI 可回到宿主声明式 Modal。已授权删除的 6 个旧文件仍可从 Git 当前基线恢复。

### 错误记录

| 时间 | 命令/步骤 | 结果 | 处理 |
|---|---|---|---|
| 2026-09-03 | `session-catchup.py` 首次调用 | 系统未注册 `python` 命令 | 改为定位工作区依赖提供的 Python 运行时，不重复相同命令 |
| 2026-09-03 | 首次追加三份规划文件 | 补丁上下文中的“错误记录”位置与文件结构不匹配，未写入任何文件 | 改用文件末尾稳定上下文追加，不重复原补丁 |
| 2026-09-03 | 读取插件注册服务候选路径 | `src/services/plugins/pluginRegistryService.ts` 不存在 | 确认注册调用直接位于 `src/store/store.plugins.ts`，使用真实路径继续 |
| 2026-09-03 | v1 Manifest 首轮定向测试/类型检查 | 早期夹具与新资源规则不一致；新测试空 inputFields 先触发字段校验；新增 resource effect 使旧分支类型不再自动收窄 | 未重复命令；统一使用当前 v1 合法夹具，并为 resource effect 增加显式执行分支 |
| 2026-09-03 | 整文件替换 `pluginFileGrantService.ts` | 同一个补丁同时 Delete/Add 相同路径，被补丁工具拒绝且未落盘 | 改为单个 Update File 或分段修改，不重复同类补丁 |
| 2026-09-03 | 首版 API v1 Manifest 回归 | 类型检查通过；仓库内测试夹具统一迁移到当前 v1 契约 | 不实现旧行为兼容解析 |
| 2026-09-03 | 原生 revision 首轮定向测试 | `cargo check` 通过；Rust 单测因测试辅助调用尚未补 `resource_payloads`、手写 revision 缺新字段而编译失败；前端 4 组回归因夹具缺 revisionDigest 大量失败 | 迁移仓库内测试夹具到唯一 v1 契约，并补完整 revision 摘要，不引入兼容兜底 |
| 2026-09-03 | 批量迁移 `tests/store/plugins.test.ts` | 一个后段上下文与文件实际缩进不一致，整批补丁未落盘 | 拆成顶部 mock、API 版本、摘要返回值和期望对象四组小补丁 |

### 2026-09-03 用户范围修订

- 用户明确“现在还没人写插件”，无需兼容既有插件内容。
- 实施调整为首版 v1 单一契约：不提供历史解析或运行兼容分支，仓库内夹具、示例和规范统一采用当前 v1。
- 不为旧插件输入保留本地 URL、旧文件 grant 或独立窗口降级入口；安全失败时显示宿主错误态。
- 删除文件仍受仓库独立检查点约束：若确认旧独立窗口文件已无引用，将先列出精确删除清单再等待确认。

### 追加发现：MCP 文档漂移

- 当前实现已支持本机 stdio 与远程 Streamable HTTP，而非旧文档中的“仅本机一次性会话”。
- 固定令牌写入 Rust `secret_store`，端口可固定或随机，会话可配置随应用自动开启；远程模式监听 `0.0.0.0/mcp`，是明文 HTTP。
- 两种传输都固定使用 C 自主模式，高影响 effect 不逐次审批；远程模式只应在受信网络或隔离环境中使用。
- 本轮已把上述现状纳入 README、AGENTS、开发指南和架构说明的同步范围，仍未扩大到产品代码。

## 2026-09-03 发布：提交 master 并向上游创建 PR

### 目标

将当前插件资源授权、主窗口内 UI、CSP 与实施记录完整提交到本地 `master`，推送到用户 fork，并向实际上游 `master` 创建或更新 PR。

### 阶段

1. [completed] 核对工作区、分支、远程、现有开放 PR 与待提交文件
2. [completed] 使用显式暂存清单提交当前全部改动
3. [completed] 推送 fork 的 `master`
4. [completed] 创建或更新 fork `master` 到上游 `master` 的 PR，并复核链接与提交范围

### 发布结果

- 实现提交：`40b230b feat(plugin): 完成资源授权与主窗口内插件界面`
- fork 分支：`luckcatlin2000/AI-Canvas-tauri:master`
- 上游 PR：`Tenney95/AI-Canvas-tauri#16`
- 本轮没有运行新的测试或启动应用；Git 提交钩子仅执行版本同步与 staged TS/TSX ESLint。

### 边界

- 按用户要求不运行新的测试或启动应用；仅做提交所需的状态、差异和远程核对。
- 不改写历史、不强推、不直接推送上游仓库。
- 提交说明使用中文；创建 PR 前避免重复的同 head/base 开放 PR。

### 发布错误记录

| 步骤 | 结果 | 处理 |
|---|---|---|
| `gh pr view 15` | 当前系统没有安装 `gh`，命令未执行 | 不新增依赖，改用现有 Git 凭据与 GitHub API/网页完成 PR 核对和创建 |
| 首次显式 `git add` | 沙箱对 `.git/index.lock` 无写权限，未暂存任何内容 | 按同一显式清单申请 Git 索引写权限后成功暂存，不改变文件范围 |
| 暂存 allowlist 首次比对 | 预期清单 48 项、实际暂存 47 项，`tests/components/toolbarRegistry.test.ts` 在 `git add` 后不再有差异 | 提交前核对该文件 working/HEAD blob；若一致则从真实提交清单排除，不制造空变更 |
| 首次读取 GitHub 凭据可用性 | 沙箱内 Git Credential Manager 无法访问 Windows 凭据存储，未取得凭据 | 在批准的宿主权限下重试，只输出存在性布尔值，确认用户名与令牌均可用且未泄露内容 |
