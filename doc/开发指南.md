# AI Canvas 开发指南

> 本文档面向开发者，涵盖本地环境搭建、项目结构、开发约定、调试技巧和常见问题。
> 当前版本：`0.9.0`；最后更新：2026-08-30

## 1. 环境准备

### 1.1 必需工具

| 工具 | 最低版本 | 说明 |
|------|---------|------|
| Node.js | `^20.19.0` 或 `>=22.12.0` | 满足 Vite 8 的运行要求 |
| npm | 随 Node.js 安装 | 包管理器 |
| Rust | stable | Tauri 编译工具链 |
| Visual Studio Build Tools | 2022 | Windows 上编译 Rust 需要 C++ 工具链 |

### 1.2 Windows 额外配置

安装 [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022)，选择「使用 C++ 的桌面开发」工作负载。

### 1.3 安装依赖

```bash
cd AI-Canvas-tauri
npm install
```

## 2. 启动开发

### 2.1 仅前端开发（不需要 Tauri 运行时）

```bash
npm run dev
```

启动后访问 `http://localhost:1420`。此模式下：

- 文件操作自动降级为浏览器 API（File Input + IndexedDB）
- 原生对话框不可用
- Tauri 官方插件功能受限
- 用户代码插件无法完成原生信任注册、QuickJS / Python 执行和启停
- 桌面端凭据存储不可用，API Key 只在当前会话有效，刷新后需要重新填写
- 项目整体导出/导入、MCP 控制桥、本地 ONNX 推理不可用
- 3D 导演台独立窗口和运行资源下载不可用

### 2.2 完整 Tauri 桌面应用开发

```bash
npm run tauri dev
```

此命令会：

1. 先启动 Vite 开发服务器（端口 1420）
2. 编译 Rust 后端
3. 打开 Tauri 桌面窗口

`npm run tauri dev` 不会拉取或启动 3D 导演台源码。首次创建并使用 `lightweight-web` 导演台节点时，应用会按正式环境的流程下载该运行时的固定构建产物；Blender 运行时使用随主应用编译内嵌的另一套资源，不走这条下载链路。

### 2.3 生产构建

```bash
npm run build          # 构建前端 (dist/)
npm run tauri build    # 构建桌面安装包
```

### 2.3.1 检查与测试

```bash
npm run typecheck        # tsc -b
npm run lint             # ESLint（见 7. 常见问题的已知阻断）
npm run test             # Vitest 全量
npm run check            # lint + typecheck + 测试类型 + 测试
npm run ci               # check + build
npx vitest run tests/services/xxx.test.ts   # 定向测试
```

测试位于 `tests/`，按 `components` / `hooks` / `services` / `store` / `utils` / `scripts` 分类，使用 Vitest 与 `fake-indexeddb`。改动服务或 Store 时优先补对应目录下的定向测试。

### 2.4 轻量网页 3D 导演台开发模式

AI Canvas 的 `lightweight-web` 导演台在开发和生产环境中都通过 `director-desk://localhost/index.html` 加载已校验的固定构建产物。主项目不使用 iframe，不启动端口 `5178`，也不依赖 Git、Node.js 或 npm 来运行该网页运行时。

需要修改导演台本体时，在独立仓库中开发：

```bash
git clone https://github.com/Tenney95/3d-director-desk.git
cd 3d-director-desk
npm install
npm run dev
```

独立仓库的 Vite 服务只用于导演台本体开发。AI Canvas 的集成验证仍应使用 GitHub Release 构建产物。

## 3. 项目约定

### 3.1 文件命名

| 类型 | 命名规则 | 示例 |
|------|---------|------|
| 组件文件 | PascalCase + `.tsx` | `TextNode.tsx` |
| Hook 文件 | `use` 前缀 + camelCase + `.ts` | `useNodeCreation.ts` |
| 服务文件 | camelCase + `Service.ts` | `fileService.ts` |
| Store 文件 | `store.` 前缀 + 职责 + `.ts` | `store.nodes.ts` |
| 样式文件 | kebab-case + `.css` | `model-selector.css` |
| 类型文件 | `index.ts` | `types/index.ts` |

### 3.2 导入顺序

```tsx
// 1. React / 框架
import { useState, useCallback } from "react";

// 2. 第三方库
import { useReactFlow } from "@xyflow/react";

// 3. 本地组件
import TextNode from "./nodes/TextNode";

// 4. Hooks
import { useAppStore } from "../store/useAppStore";

// 5. 类型
import type { BaseNodeData } from "../types";
```

### 3.3 组件编写规范

- 节点组件用 `React.memo` 包裹
- 接收 `{ data, selected }` props
- 数据变更通过 `useAppStore().updateNodeData(nodeId, data)`
- 禁止在节点组件内直接修改 store 状态

```tsx
// ✅ 正确
const TextNode = React.memo(({ data, selected }: NodeProps) => {
  const updateNodeData = useAppStore((s) => s.updateNodeData);
  // ...
});

// ❌ 错误
const TextNode = ({ data }: NodeProps) => {
  useAppStore.setState({ ... }); // 禁止
};
```

### 3.4 类型定义

通用画布类型定义在 `src/types/index.ts`：
- `NodeType` — 节点类型联合类型
- `BaseNodeData` — 节点数据接口
- `CanvasProject` — 项目元数据
- `AppConfig` / `ApiProviderConfig` — API 配置
- `GeneralModelConfig` — 用户自建通用模型
- `ModelOption` / `ModelGroup` — 模型选项
- `WorkflowDefinition` — ComfyUI 工作流
- `UserPreset` — 用户预设
- `NodeGroup` — 节点分组

领域类型按模块拆分：`chat.ts`、`agent.ts`、`subAgent.ts`、`mcp.ts`、`plugin.ts`、`agentPackage.ts`、`canvasNote.ts`、`dramaAssets.ts`、`media.ts`、`memory.ts`、`aiTypes.ts`、`composerTypes.ts`。

新增类型优先加到对应文件，禁止在组件文件中重复定义。持久化类型不得包含 `AbortController`、本地绝对路径、密钥或 MCP 令牌。

### 3.5 样式约定

- 业务样式优先使用 Tailwind class
- 禁止 `!important`、硬编码颜色值、内联 `style.cssText`
- 复用 `tailwind.config.js` 中的 `canvas-*` 颜色 token（暗色基线）
- 浅色主题是低饱和马卡龙配色，由 `src/styles/base.css` 中的 `[data-theme='light']` 覆盖；新增可见面板必须同时确认两种主题
- React Flow 样式覆盖统一放 `src/styles/reactflow.css`
- 新增组件独立样式放入 `src/styles/` 目录，并在 `src/index.css` 中 `@import`
- 滚动条由 `OverlayScrollbarLayer` 统一接管，不要给单个容器加原生滚动条样式
- 节点类型 Header 使用语义色：文本=indigo、图像=green、视频=blue、音频=orange、全景=cyan

### 3.6 状态管理约定

- 所有状态变更必须通过 Store Actions
- 不可变更新：使用扩展运算符或 `immer`-style 更新
- 新增 slice 在 `src/store/` 下新建 `store.xxx.ts`，在 `useAppStore.ts` 中聚合
- Slice 会进入主窗口静态启动图，顶层不得导入模型协议、Provider、完整 Agent Runtime 或编辑器运行时
- 删除前清理、同步取消和状态迁移必须使用轻量服务，不能等待动态导入
- 组件内选择器保持精简，避免不必要的重渲染：

```tsx
// ✅ 精确选择
const nodes = useAppStore((s) => s.nodes);

// ❌ 全量选择（任何状态变更都会重渲染）
const store = useAppStore();
```

### 3.7 节点快捷键

画布快捷键由 `src/hooks/useKeyboardShortcuts.ts` 统一处理。不要在节点组件中新增全局 `keydown` 监听。

| 快捷键 | 作用 |
|---|---|
| `1` 到 `7`（含小键盘） | 创建文本、图像、视频、音频、全景、动画、3D 导演台节点 |
| `Alt + 1` 到 `Alt + 5` | 创建文本、图像、视频、音频、Markdown 源节点 |
| `Ctrl/Cmd/Alt + S` | 保存项目 |
| `Ctrl/Cmd + C` / `V` | 复制 / 粘贴节点 |
| `Ctrl/Cmd + Z`、`Ctrl/Cmd + Shift + Z`、`Ctrl/Cmd + Y` | 撤销 / 重做 |
| `Ctrl/Cmd/Alt + G` | 分组 / 取消分组 |
| `Ctrl/Cmd + Shift + M` | 切换吉祥物显示 |
| `Delete` / `Backspace` | 删除选中 |
| `Space` | 单选节点时打开 AI 对话框 |
| `F` / `M` / `Escape` | 适应视图 / 小地图 / 关闭浮层 |
| `F12` | 开发者工具 |

画布笔记工具通过 `CanvasDrawingToolbar` 选择，不占用数字快捷键。

新增数字快捷键时，同步更新 `CanvasContextMenu.tsx` 和 `Sidebar.tsx` 的帮助说明。创建入口必须复用同一组节点尺寸、角色和初始状态。

### 3.8 启动依赖与运行时分层

主窗口启动图包含 Store、全局 Hook 和首屏组件的静态依赖。提交代码前，按以下规则选择导入位置：

1. 把同步取消、状态迁移和运行时注册表放入轻量控制模块。
2. 让 Store Slice 直接调用轻量模块，保持 Action 的同步或异步契约。
3. 把模型上下文、流式协议、Provider 和工具执行器留在懒加载功能边界。
4. 不要用 `manualChunks` 掩盖静态依赖。模块仍在启动图时，分 chunk 不会减少启动解析成本。
5. 不要把同步中止改成 `import()`。异步加载会产生删除后的任务写回窗口。

Agent 的当前分层可作为参考：`agentTaskControl.ts` 进入共享启动 chunk，`agentRuntime.ts` 由 ChatPanel 按需加载，`agentRoundExecutor.ts` 与 `contextManager.ts` 落在独立的按需 chunk。`agentRuntime.ts` 重导出控制 API，既有聊天调用保持兼容；Store 应直接导入 `agentTaskControl.ts`。

### 3.9 安全边界约定

以下边界不是可选优化，绕过任何一条都会把 Renderer 的一次注入放大成系统级读写：

1. **凭据**：API Key 只经 `services/providerSecretService.ts` 交给 Rust `secret_store`。配置里只留条目名，禁止把明文写回 IndexedDB、导出归档、消息或日志。
2. **原生路径命令**：新增接收路径参数的自定义 command 必须调用 `path_policy.rs` 做调用方窗口校验和真实路径校验。自定义命令不经过 fs 插件 scope，不校验等于任意文件读写。
3. **Agent 出网**：只走 `assistant_web.rs`（搜索与网页读取）和 `provider_docs.rs`（厂商文档）。通用 `proxy_fetch` 不注册为 Agent 工具。
4. **工具权限**：新工具只能 `registerAgentTool()` 注册并声明准确的 effect；Policy Engine 的矩阵是本地固定边界，任何模型输出、网页、Skill 或 MCP 输入都不能修改它。
5. **用户插件**：安装与更新必须经过 `stage → IndexedDB persist → activate`。普通执行只提交 `pluginId + sourceDigest + toolId + invocationId + input`，源码和 runtime 由 Rust 私有注册表读取并重新核对摘要。JavaScript 在独立 QuickJS Runtime 中运行，远程媒体写回受来源集合约束；Python 是拥有当前用户完整系统权限的可信本机代码。`plugin-private` 必须同时拒绝 fs、asset protocol 和通用路径命令访问。
6. **MCP**：只暴露当前项目中已注册且可用的 Agent 工具，并固定使用 C 自主模式。`read`、`canvas_write`、`file_write`、`permanent_delete`、`media_generation`、`memory_write`、`config_write` 和 `asset_write` 均直接执行，不进入 B 模式审批；会产生模型开销的工具（如 `agent_run_sub_agent`）也可发现并执行。MCP 不能调用审批解决接口。工具的 `isAvailable` 不得依赖 `context.taskId`——发现阶段没有真实任务，会让工具从 MCP 列表里静默消失；需要任务上下文的判断放进 `authorize`。

## 4. 目录结构

```
AI-Canvas-tauri/
├── index.html                      # Vite 入口 HTML
├── src/
│   ├── main.tsx                    # React 应用入口
│   ├── App.tsx                     # 根组件装配
│   ├── index.css                   # 全局样式入口（导入所有 partial）
│   ├── assets/                     # 静态资源
│   │   └── images/
│   │       ├── bg/                 # 画布背景图
│   │       └── styles/             # 风格参考图
│   ├── constants/
│   │   └── api.ts                  # API 端点常量
│   ├── components/
│   │   ├── Canvas.tsx              # React Flow 画布主组件
│   │   ├── Header.tsx              # 顶部栏
│   │   ├── Sidebar.tsx             # 左侧面板
│   │   ├── Titlebar.tsx            # 自定义窗口标题栏
│   │   ├── NodeMenu.tsx            # 浮动节点菜单
│   │   ├── SettingsPanel.tsx       # 设置弹窗
│   │   ├── WorkflowPanel.tsx       # 工作流管理面板
│   │   ├── AssetsPanel.tsx         # 资产管理面板
│   │   ├── AssetSearchWindow.tsx   # 资源搜索浮窗
│   │   ├── OutputHistoryPanel.tsx  # 输出历史记录面板
│   │   ├── ProjectLibraryModal.tsx # 项目库卡片视图
│   │   ├── SessionProjectTabs.tsx  # 项目标签栏
│   │   ├── ProjectSettingsPopover.tsx # 项目创作基线设置
│   │   ├── SeriesRail.tsx          # 右缘剧集栏：原著、剧本、分集与本集大纲
│   │   ├── ProjectAssetsOverlay.tsx # 按集查看整部剧的图片与视频
│   │   ├── ProjectSwitchOverlay.tsx # 切换画布时的 Logo 加载遮罩
│   │   ├── CharacterLibraryPanel.tsx  # 角色库面板
│   │   ├── CharacterAssetDialog.tsx   # 角色卡编辑与头像裁切
│   │   ├── DramaAssetsPanel.tsx    # 短剧人物/场景/道具资产
│   │   ├── Toast.tsx               # 消息提示
│   │   ├── SplashScreen.tsx        # 开屏动画
│   │   ├── OnboardingDialog.tsx    # 首次启动引导
│   │   ├── HelpCenterDialog.tsx    # 帮助中心
│   │   ├── HelpMentionDemo.tsx     # 帮助中心的 @ 节点动画演示
│   │   ├── backgrounds/            # 画布背景组件
│   │   ├── canvas/                 # 画布交互子组件
│   │   │   ├── CanvasContextMenu.tsx
│   │   │   ├── NodeContextMenu.tsx
│   │   │   ├── ConnectionMenu.tsx
│   │   │   ├── CanvasToolbar.tsx
│   │   │   ├── CanvasDrawingToolbar.tsx   # 笔记与绘图工具选择
│   │   │   ├── CanvasNoteStylePanel.tsx   # 笔记样式设置
│   │   │   ├── HistoryTimelinePanel.tsx   # 操作记录浮层
│   │   │   ├── SelectedNodeFlowEdge.tsx
│   │   │   ├── RoundedMiniMapMask.tsx
│   │   │   ├── CanvasEmptyState.tsx
│   │   │   ├── DistributionGapHandles.tsx
│   │   │   └── MultiSelectToolbar.tsx
│   │   ├── noteNodes/              # 画布笔记渲染
│   │   │   ├── CanvasNoteNode.tsx
│   │   │   ├── CanvasNoteShape.tsx
│   │   │   ├── CanvasNoteText.tsx
│   │   │   └── CanvasNoteImage.tsx
│   │   ├── character/              # 角色参考图与声音素材展示
│   │   ├── director/               # 导演台下载确认、进度和安装编排
│   │   ├── chat/                   # 对话助手组件
│   │   │   ├── ChatPanel.tsx / ChatWindow.tsx
│   │   │   ├── ChatHeader.tsx / ChatInput.tsx / ChatMessages.tsx
│   │   │   ├── ChatComposerEditor.tsx / ChatMarkdown.tsx / ChatReferenceText.tsx
│   │   │   ├── ConversationList.tsx / MessageBubble.tsx / EmptyChatState.tsx
│   │   │   ├── AgentModeSelector.tsx      # Plan / B / C 模式
│   │   │   ├── AgentTaskTimeline.tsx / AgentStepCard.tsx / AgentApprovalCard.tsx
│   │   │   ├── AgentTaskCenter.tsx        # 跨会话任务汇总
│   │   │   ├── SubAgentPanel.tsx          # 子智能体配置入口
│   │   │   ├── ContextUsageIndicator.tsx / ProjectMemoryPanel.tsx / SourceList.tsx
│   │   │   └── MascotAvatar.tsx
│   │   ├── settings/               # 设置子组件
│   │   │   ├── ApiKeySettings.tsx / ProviderConnectionDialog.tsx
│   │   │   ├── ModelProtocolEditor.tsx / ProtocolImportPanel.tsx
│   │   │   ├── McpControlSettings.tsx     # MCP 传输、端口、令牌、自动开启与状态
│   │   │   ├── PluginSettings.tsx          # 用户插件安装、更新与启停
│   │   │   ├── SubAgentSettings.tsx
│   │   │   ├── StorageHealthCenter.tsx / DirectorDeskStorageManager.tsx
│   │   │   └── DreaminaLoginModal.tsx
│   │   ├── shared/                 # 通用共享组件
│   │   │   ├── AnimatedButton.tsx / AssetThumb.tsx / ZoomableImage.tsx
│   │   │   ├── FullscreenOverlay.tsx / ModalOverlay.tsx / PopupCloseButton.tsx
│   │   │   ├── LazyLoadBoundary.tsx / OverlayScrollbarLayer.tsx
│   │   │   ├── ModelDownloadDialog.tsx
│   │   │   └── mascot/             # 吉祥物组件
│   │   └── nodes/                  # 节点组件
│   │       ├── TextNode.tsx / ImageNode.tsx / VideoNode.tsx / AudioNode.tsx
│   │       ├── AnimationNode.tsx / PanoramaNode.tsx / MarkdownNode.tsx
│   │       ├── StoryboardNode.tsx / DirectorDeskNode.tsx / PluginNode.tsx / GroupNode.tsx
│   │       ├── AINodeDialog.tsx    # AI 生成统一弹窗
│   │       ├── panorama/           # 全景查看器封装
│   │       └── shared/             # 节点共享子组件
│   │           ├── defaultModels.ts
│   │           ├── ModelSelector.tsx / MentionEditor.tsx / PromptPanel.tsx
│   │           ├── SlashCommandMenu.tsx / slashCommands.ts
│   │           ├── PresetManager.tsx / PresetAdvancedEditor.tsx / PresetRunnerDialog.tsx
│   │           ├── SkillManager.tsx / StyleSelector.tsx / QualityRatioSelector.tsx
│   │           ├── VideoParamSelector.tsx / VideoNodeControls.tsx
│   │           ├── AudioParamSelector.tsx / AudioNodeToolbar.tsx
│   │           ├── NodeLabel.tsx / NodeError.tsx / ResizeHandle.tsx
│   │           ├── TextNodeToolbar.tsx / VideoNodeToolbar.tsx / PanoramaNodeToolbar.tsx
│   │           ├── ConnectedNodesPreview.tsx / GooeyBtn.tsx
│   │           ├── useNodeRename.ts / useSourceFileUpload.ts
│   │           ├── toolbar/        # 可编辑工具栏
│   │           │   ├── ToolbarEditor.tsx
│   │           │   ├── ToolbarMoreMenu.tsx
│   │           │   ├── toolbarRegistry.ts
│   │           │   └── presetAction.ts
│   │           └── image/          # 图片节点工具链
│   │               ├── ImageNodeToolbar.tsx
│   │               ├── CropEditor.tsx / cropUtils.ts / PenCropLayer.tsx
│   │               ├── CustomGridEditor.tsx / ExpandEditor.tsx
│   │               ├── MattingEditor.tsx / MattingToolbar.tsx
│   │               ├── CameraStudioPanel.tsx / cameraStudio.ts
│   │               ├── ImageGenerationHistoryDialog.tsx
│   │               ├── ImageEditorZoomControls.tsx / imageUtils.ts
│   │               └── composer/   # 图片合成编辑器
│   ├── hooks/                      # 自定义 Hooks
│   │   ├── useNodeCreation.ts / useKeyboardShortcuts.ts
│   │   ├── useCanvasDrawing.ts     # 笔记绘制交互
│   │   ├── useCanvasContextMenu.ts / useNodeContextMenu.ts / useConnectionDropMenu.ts
│   │   ├── useNodeSnap.ts / useAutoSave.ts / useCompletionFlash.ts
│   │   ├── useToolbarEdit.ts / useTooltipAutoPlacement.ts
│   │   ├── useReferencedImageWatcher.ts
│   │   ├── useMascotDrag.ts / useMascotStatus.ts
│   │   ├── useImageViewportGesture.ts
│   │   └── useShiftProportional.ts
│   ├── services/                   # 服务层
│   │   ├── aiService.ts            # AI 服务聚合入口
│   │   ├── fileService.ts          # 文件读写封装
│   │   ├── indexedDbService.ts     # IndexedDB 核心 CRUD 门面
│   │   ├── indexedDb/              # Schema 真值与领域 Repository
│   │   │   ├── schema.ts           # DB_VERSION=20、object store 与升级入口
│   │   │   └── catalogRepository.ts / videoEditorRepository.ts
│   │   ├── providerSecretService.ts # API Key ↔ Rust 凭据存储
│   │   ├── projectTransferService.ts # 项目整体导出 / 导入
│   │   ├── projectSnapshotService.ts / projectSnapshotWorker.ts # 画布缩略图
│   │   ├── projectSettingsService.ts # 项目创作基线解析
│   │   ├── characterLibraryService.ts / characterReferenceMerge.ts # 角色库
│   │   ├── dramaAssetExtract.ts / dramaAssetPrompt.ts # 短剧资产
│   │   ├── canvasDerivationGuard.ts # 过期派生结果拦截
│   │   ├── canvasViewportService.ts / canvasPointerService.ts
│   │   ├── generationService.ts / imageBatchService.ts
│   │   ├── presetSequenceService.ts / presetTemplateService.ts
│   │   ├── skillPromptService.ts   # Skill 提示词展开
│   │   ├── webSearchService.ts / webPageService.ts / providerDocsService.ts
│   │   ├── clipboardService.ts / uploadService.ts / testConnection.ts
│   │   ├── storageService.ts / updateService.ts / backgroundService.ts
│   │   ├── directorDeskService.ts / directorDeskWindowService.ts / directorDeskRuntimeService.ts
│   │   ├── directorRuntimeRegistry.ts / directorBlenderRuntimeService.ts / directorSceneService.ts
│   │   ├── nodeReferenceService.ts / apimartService.ts / comfyWorkflowService.ts
│   │   ├── dreaminaService.ts / onnxService.ts / aiDimensions.ts
│   │   ├── pollManager.ts / pollTask.ts
│   │   ├── ai/                     # AI 生成子模块
│   │   │   ├── generationRuntime.ts
│   │   │   ├── generateText.ts / generateImage.ts / generateVideo.ts / generateAudio.ts
│   │   │   ├── mediaProviderRegistry.ts / connectedReferenceMedia.ts
│   │   │   ├── modelProtocol.ts / modelProtocolRuntime.ts / modelProtocolImport.ts
│   │   │   ├── providerCatalogService.ts
│   │   │   ├── apimartGen.ts / apimartAudio.ts / apimartVideoModels.ts
│   │   │   ├── assistantStream.ts / streamParsers.ts
│   │   │   ├── transcribeAudio.ts / animationPrompt.ts / panoramaPrompt.ts
│   │   │   ├── promptResolver.ts / helpers.ts / batchUtils.ts
│   │   │   ├── httpTransport.ts / httpUtils.ts / imageUtils.ts
│   │   │   └── providers/          # 厂商 Provider 适配器
│   │   │       ├── standardImage.ts / volcengineImage.ts
│   │   │       ├── apimartMedia.ts / runninghubImage.ts
│   │   ├── chat/                   # 对话助手服务
│   │   │   ├── agentTaskControl.ts # 轻量任务控制、同步中止和状态迁移
│   │   │   ├── agentRuntime.ts     # 按需模型、上下文和工具执行循环
│   │   │   ├── agentRoundExecutor.ts / agentToolExecution.ts / agentToolSchemas.ts
│   │   │   ├── agentScheduler.ts / conversationExecutionController.ts
│   │   │   ├── agentBudgetService.ts / agentCheckpointService.ts / agentRewindService.ts
│   │   │   ├── agentInterjection.ts / agentJournal.ts / agentLifecycle.ts / agentErrorCodes.ts
│   │   │   ├── toolRegistry.ts / policyEngine.ts / rulesEngine.ts
│   │   │   ├── subAgentService.ts / subAgentProfileService.ts / subAgentMaterials.ts
│   │   │   ├── expertTaskService.ts / canvasPlanner.ts
│   │   │   ├── contextManager.ts / contextCompressionService.ts / tokenEstimate.ts
│   │   │   ├── skillCatalog.ts / skillManifest.ts / promptLearningService.ts
│   │   │   ├── fileGrantService.ts / webAccessGrantService.ts / providerDocsGrantService.ts
│   │   │   ├── projectMemoryService.ts / memoryRetrieval.ts
│   │   │   ├── mediaPlaceholderLifecycle.ts / providerConfigDraftService.ts
│   │   │   ├── chatHistoryService.ts / chatWindowService.ts / detachedChatSyncController.ts
│   │   │   ├── commandRegistry.ts / assistantService.ts
│   │   │   └── tools/              # 画布、媒体、预设、文件、联网、Skill、厂商配置、
│   │   │                           # 短剧资产、剧集分集、子智能体和记忆工具
│   │   ├── mcp/                    # MCP 控制、双传输与会话配置
│   │   │   ├── mcpControlService.ts
│   │   │   ├── mcpBridgeService.ts
│   │   │   └── mcpSessionConfig.ts
│   │   ├── plugins/                # 用户插件 Manifest、市场、执行租约与文件授权
│   │   │   ├── pluginManifest.ts / pluginMarketplace.ts
│   │   │   └── pluginRuntime.ts / pluginFileGrantService.ts
│   │   ├── agentPackages/          # 全局 Agent Package 导入、目录与 Skill 绑定
│   │   └── fs/                     # 文件系统抽象层
│   │       ├── core.ts / assetLibrary.ts / assetIndex.ts
│   │       ├── generatedAssetTags.ts / storageHealth.ts
│   │       └── trash.ts
│   ├── store/                      # Zustand 状态管理（21 个 slice）
│   │   ├── useAppStore.ts          # 聚合入口
│   │   ├── store.nodes.ts / store.ui.ts / store.toast.ts
│   │   ├── store.history.ts / store.historyRecord.ts
│   │   ├── store.config.ts / store.workflows.ts / store.presets.ts
│   │   ├── store.skills.ts / store.styles.ts / store.subAgents.ts
│   │   ├── store.groups.ts / store.clipboard.ts / store.projects.ts
│   │   ├── store.chat.ts / store.agent.ts / store.memory.ts
│   │   ├── store.toolbar.ts / store.dramaAssets.ts
│   │   ├── store.plugins.ts / store.agentPackages.ts
│   │   └── store.utils.ts
│   ├── types/                      # 类型定义
│   │   ├── index.ts                # 全局类型（节点、项目、配置等）
│   │   ├── aiTypes.ts / media.ts / composerTypes.ts
│   │   ├── chat.ts / agent.ts / memory.ts
│   │   ├── subAgent.ts / mcp.ts / plugin.ts / agentPackage.ts
│   │   └── canvasNote.ts / dramaAssets.ts
│   ├── styles/                     # 组件样式（CSS partials，由 index.css 聚合）
│   │   ├── base.css                # 含 [data-theme='light'] 浅色覆盖
│   │   ├── reactflow.css
│   │   ├── nodes*.css              # 按媒体类型拆分
│   │   ├── canvas-drawing.css / canvas-context.css
│   │   ├── character-library.css / camera-studio.css
│   │   ├── panels.css / sidebar.css / settings.css / separators.css
│   │   ├── model-selector.css / prompt.css / preset-manager.css
│   │   ├── quality-ratio.css / tooltip.css / toolbar-editor.css
│   │   └── crop.css / composer.css / backgrounds.css
│   └── utils/                      # 工具函数
│       ├── batchExecute.ts / distributionGeometry.ts / nodeBounds.ts
│       ├── canvasNoteGeometry.ts   # 笔记几何
│       ├── historyOperationLabels.ts
│       ├── popupPosition.ts / dropCapture.ts / nodeAnimations.ts / motion.ts
│       ├── num.ts / textSelection.ts / renderMarkdown.ts
│       └── assetDrag.ts / assetFormat.ts / assetSearchWindow.ts
├── src-tauri/                      # Tauri Rust 后端
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── resources/
│   │   └── blender-runtime/v1/     # 内嵌 Blender Manifest、模板、人物与固定 Job
│   └── src/
│       ├── main.rs                 # Rust 入口
│       ├── lib.rs                  # Tauri Builder 装配
│       ├── path_policy.rs          # 原生命令调用方与路径校验
│       ├── secret_store.rs         # Rust 独占的本地凭据存储
│       ├── mcp_bridge.rs           # MCP stdio 回环桥与 Streamable HTTP 服务
│       ├── project_archive.rs      # 项目归档打包与解包
│       ├── assistant_web.rs        # 受限网页搜索与读取
│       ├── provider_docs.rs        # 受限厂商文档读取
│       ├── director_desk_runtime.rs # 导演台下载、校验、安装和本地协议
│       ├── blender_runtime.rs / blender_runtime/ # Blender 安装、授权、Job 与结果校验
│       ├── plugin_registry.rs       # 插件私有注册表、摘要与原生启停
│       ├── plugin_runtime.rs        # QuickJS / 可信 Python 执行与取消
│       ├── file_transfer.rs        # 可取消文件传输
│       ├── clipboard.rs            # 系统剪贴板文件写入
│       ├── dreamina.rs             # 即梦 API 封装
│       ├── comfyui/
│       │   └── mod.rs              # ComfyUI 后端接口
│       └── onnx/                   # ONNX 本地推理
│           ├── mod.rs
│           ├── config.rs
│           ├── gpu.rs
│           └── worker.rs
├── tests/                          # Vitest 测试
│   ├── components/ hooks/ services/ store/ utils/ scripts/
│   └── setup.ts
├── doc/                            # 项目文档（含 adr/ 与 plans/）
├── scripts/                        # Git Hook、版本同步、MCP 适配器和导演台 Release 清单
│   ├── ai-canvas-mcp.mjs           # stdio MCP 适配器
│   ├── director-desk-release.json
│   ├── sync-version.js / setup-hooks.js / pre-commit
│   └── update-director-desk-release.mjs
├── tailwind.config.js
├── vite.config.ts
├── vitest.config.ts
├── tsconfig.json
└── package.json
```

## 5. 调试技巧

### 5.1 React DevTools

安装 React DevTools 浏览器扩展，开发模式下在浏览器控制台使用。

### 5.2 React Dev Inspector

开发模式下按 `Ctrl+Shift+C` 可以点击页面元素跳转到源码位置（需要 IDE 支持 `codebuddycn://` 协议）。

### 5.3 Tauri 调试

```bash
# 查看 Tauri 日志
npm run tauri dev

# Rust 后端日志会输出到终端
# 前端 console.log 也会同步到终端
```

### 5.4 IndexedDB 调试

在浏览器 DevTools → Application → IndexedDB → `ai-canvas-db` 查看持久化数据。当前 schema 版本为 `20`，唯一真值是 `src/services/indexedDb/schema.ts` 的 `DB_VERSION`；`indexedDbService.ts` 是核心 CRUD 门面，不再拥有 schema 版本。

API Key 不在这里：凭据由 Rust 保管在 `{appData}/secrets/credentials.json`，配置里只有条目名。想确认凭据是否落盘，看该文件是否存在即可，不要尝试从 Renderer 读取——三条访问路径都会拒绝。

### 5.5 Zustand DevTools

可以在 `create()` 中启用 Redux DevTools 集成调试状态变更。

### 5.6 3D 导演台双运行时调试

先确认当前固定版本和产物信息：

```bash
node -e "console.log(require('./scripts/director-desk-release.json'))"
```

在 Tauri 窗口中按 `7` 或通过画布右键菜单创建导演台节点。首次使用 `lightweight-web` 运行时会显示下载确认，安装状态可在 **设置 > 存储健康 > 3D 导演台资源** 中查看；Blender 使用独立的编译内嵌资源安装链路。

运行以下定向检查：

```bash
npx vitest run tests/services/directorDeskRuntimeService.test.ts
npx vitest run tests/services/directorDeskWindowService.test.ts
cargo test --manifest-path src-tauri/Cargo.toml director_desk_runtime::tests --lib
```

浏览器模式没有 Tauri 本地协议和独立窗口，不能用于导演台集成验证。

Blender 预览必须在 Windows Tauri 应用中验证。当前内嵌运行包为 `1.3.2`，锁定 Blender 5.2.1 LTS；版本真值必须以 `src-tauri/resources/blender-runtime/v1/runtime-manifest.json` 与 `src-tauri/src/blender_runtime/resources.rs` 的同步结果为准。标准安装可以有界发现，Steam/便携版在 **设置 → 文件与应用 → 外部编辑器** 手选 `blender.exe`。界面只显示候选摘要，绝对路径不会写入项目或 IndexedDB。

Blender 相关定向检查：

```bash
cargo test --manifest-path src-tauri/Cargo.toml --no-default-features blender_runtime:: --lib
cargo check --manifest-path src-tauri/Cargo.toml --lib
npx vitest run tests/services/directorBlenderRuntimeService.test.ts tests/services/directorRuntimeRegistry.test.ts
npm run typecheck
```

真机最小回归按同一 `ai-director` 节点依次验证：手选候选、打开高级编辑、独立同步当前帧、导出参考视频、取消任务和保存返回。“保存并返回”本身必须同时生成一个新的 `.blend` artifact 和当前活动摄影机帧 PNG；Rust 对两个 artifact 的类型、数量、路径、大小与 SHA-256 独立校验，前端再把该帧追加到截图历史，并投影为节点最新 `imageUrl`、`thumbnailUrl` 和 Result Manifest。独立“同步当前帧”仍保留，但不是保存返回后获得预览的前置步骤。

没有活动摄影机、当前帧越界或保存/渲染失败时，本次 `open-editor` 不生成可收集的 Result Manifest，不回写节点，也不覆盖此前已验证输出；Blender 失败不得自动切换到 `lightweight-web`。错误信息不得包含项目绝对路径、可执行文件路径或私有 Job 目录。

固定资源位于 `src-tauri/resources/blender-runtime/v1/`，包括 manifest、Application Template 的 `__init__.py` / `startup.blend`、内置男女白模及许可证和固定 Job 脚本。更新任一资源时必须同步 bytes/SHA-256、重新运行嵌入资源测试并完成 Blender 真机兼容回归；不要提交 `__pycache__`、`.blend1`、临时 Job 输入输出或 bootstrap 试验脚本。

### 5.7 启动依赖与 sourcemap 调试

性能拆包必须验证源码归属，不能只比较 chunk 文件名。Windows PowerShell 可在系统临时目录生成生产 sourcemap：

```powershell
$perfOut = Join-Path $env:TEMP 'ai-canvas-perf-build'
npx vite build --sourcemap --outDir $perfOut --emptyOutDir
rg -l 'src/services/chat/agentRuntime.ts' "$perfOut/assets" --glob '*.map'
rg -l 'src/services/chat/agentTaskControl.ts' "$perfOut/assets" --glob '*.map'
```

检查结果应满足以下条件：

- `agentTaskControl.ts` 出现在共享启动 chunk
- `agentRuntime.ts` 只出现在 ChatPanel chunk，`agentRoundExecutor.ts` 与 `contextManager.ts` 只出现在按需 chunk
- 生产构建完成，且没有新增无效动态导入警告
- 相关同步 Action 的回归测试通过

chunk 体积和文件名会随功能与内容哈希变化。每次性能复核都应记录当次构建统计，并以 sourcemap 中的源码路径判断归属；不要把旧版本数值或 chunk 文件名当作长期基线。

### 5.8 MCP 控制桥调试

MCP 默认关闭，可以在 **设置 > MCP 控制** 手动开启，也可以显式启用“启动软件时自动开启”。端口可留空让每次会话随机分配，或配置 `1024–65535` 的固定端口。256-bit 固定令牌由 `mcpSessionConfig.ts` 生成并写入 Rust `secret_store` 的 `mcp/token` 条目，不进入 IndexedDB；凭据存储不可用时才退回本次会话令牌。令牌泄露或需要废止旧客户端配置时使用设置页的轮换操作，随后重新复制客户端配置。

两种传输最终都进入同一个 `mcpControlService.ts`、Tool Registry、Policy 和 AgentTask 审计链：

1. **本机 stdio**：默认传输。`scripts/ai-canvas-mcp.mjs` 通过 `127.0.0.1` 的固定或随机端口连接 Rust bridge；客户端配置把令牌放进 `AI_CANVAS_MCP_TOKEN` 环境变量，不写入命令行参数。
2. **远程 Streamable HTTP**：服务监听 `0.0.0.0:<port>/mcp`，允许局域网、Docker 或其他可达主机连接。请求必须携带 `Authorization: Bearer <token>`；Rust 还限制 Host 为 IPv4、`localhost` 或受支持的 Docker 主机名，有 Origin 时要求同主机同端口，并把请求体限制为 1 MiB、并发限制为 4。当前服务是明文 HTTP，Bearer Token 会随请求发送；只应在受信网络或隔离环境中开启，不要直接暴露到公网。
3. **执行语义**：stdio 与 Streamable HTTP 都固定使用 C 自主模式。已注册且通过 `isAvailable` / `authorize` 的读、画布写、文件写、永久删除、媒体生成、记忆写、配置写和资产写 effect 均不逐次审批；产生模型开销的可用工具（如 `agent_run_sub_agent`）也能执行。`user_choice` 仍表示等待用户作答，不是风险审批；MCP 客户端不能调用审批解决接口。
4. 每次调用都会在当前项目的「MCP 控制」会话中生成 AgentTask，可在任务时间线核对 Tool Registry 可用性、Policy 决策、执行与脱敏结果。
5. 定向检查：

```bash
npx vitest run tests/scripts/aiCanvasMcp.test.mjs tests/components/mcpControlSettings.test.ts tests/services/mcp/mcpControlService.test.ts
cargo test --manifest-path src-tauri/Cargo.toml --no-default-features mcp_bridge::tests --lib
```

工具在 MCP 列表中意外消失时，先查该工具的 `isAvailable` 是否依赖了 `context.taskId`：发现阶段用的是占位 taskId，查不到任务就会静默过滤掉它。

### 5.9 用户插件平台调试

用户插件的安装、启停和执行依赖 Rust 私有注册表，因此必须在 Tauri 桌面窗口中验证。浏览器模式可以渲染设置界面，但不能把它当作 QuickJS、可信 Python 或原生启停边界的验收环境。

前端定向检查：

```bash
npx vitest run tests/services/pluginManifest.test.ts tests/services/pluginRuntime.test.ts tests/services/pluginMarketplace.test.ts tests/services/pluginFileGrantService.test.ts tests/store/plugins.test.ts
npm run typecheck
```

Rust 定向检查应避开与插件无关的可选 ONNX 链路：

```bash
cargo test --manifest-path src-tauri/Cargo.toml --no-default-features plugin_registry:: --lib
cargo test --manifest-path src-tauri/Cargo.toml --no-default-features plugin_runtime:: --lib
cargo check --manifest-path src-tauri/Cargo.toml --no-default-features --lib
```

手工回归至少覆盖本地安装、GitHub 安装、同 ID 更新、停用、重新启用和卸载。Python 安装、更新和从停用状态重新启用必须先显示前端源码摘要与完整本机权限警告，再显示绑定同一完整摘要的 Rust 原生 Warning。更新后旧版本结果不得继续请求宿主 effect 或写回画布；停用、卸载和摘要切换必须先撤销前端版本租约与文件授权，再由原生层阻止新调用并取消活动 Python 调用。

不要为了调试读取或编辑 `plugin-private`。设置页显示的 `sourceDigest`、原生命令结果、定向测试和失败关闭行为才是可观察接口；已有摘要但原生注册缺失、摘要不符或快照损坏时，插件必须自动停用，不能退回执行 IndexedDB 中的源码。完整插件作者契约见[插件开发规范](./插件开发规范.md)，原生边界见[原生信任注册表 ADR（0010-native-plugin-trust-registry.md）](./adr/0010-native-plugin-trust-registry.md)。

## 6. 新增功能指南

### 6.1 新增节点类型

1. 在 `src/types/index.ts` 的 `NodeType` 中添加新类型
2. 创建 `src/components/nodes/NewTypeNode.tsx`
3. 在 `src/components/Canvas.tsx` 的 `nodeTypes` 映射中注册
4. 在 `src/components/Sidebar.tsx` 的节点列表中添加入口
5. 在 `src/hooks/useNodeCreation.ts` 中添加创建方法
6. 在 `src/services/aiService.ts` 中添加对应的生成适配器（如需要）
7. 在 `src/components/nodes/shared/toolbar/toolbarRegistry.ts` 中登记该类型的工具栏动作（如需要）

### 6.2 新增 AI 提供商

1. 在 `src/services/aiService.ts` 的 `DEFAULT_BASE_URLS` 中添加 base URL
2. 在 `src/components/nodes/shared/defaultModels.ts` 中添加模型配置
3. 在 `src/components/settings/ApiKeySettings.tsx` 中添加提供商配置入口
4. 实现对应的请求/响应适配逻辑：优先扩展 `services/ai/providers/` 下的 adapter，能用声明式描述的走 `services/ai/modelProtocol.ts`
5. 如果该厂商支持参考图片、视频或音频，在 `services/ai/mediaProviderRegistry.ts` 中声明能力，不要在节点组件里判断
6. 凭据一律经 `providerSecretService.ts` 存取，不要新增读写 `config.providers[*].apiKey` 明文的路径

### 6.2.1 新增 Agent 工具

1. 在 `src/services/chat/tools/` 下的对应文件中调用 `registerAgentTool()`
2. 声明本地 `inputSchema` 与准确的 `effect`；`effect` 决定 Policy 是自动执行还是必须确认
3. 需要任务上下文的可用性判断放进 `authorize`，不要放进 `isAvailable`（会破坏 MCP 工具发现）
4. 审计普通 Agent 与 MCP 两个入口：所有工具都通过同一套 `isAvailable` / `authorize`、本地 schema 和 Policy；MCP 固定为自主模式，不按“是否产生模型开销”维护隐藏排除列表
5. 补 `tests/services/chat/` 下的定向测试

### 6.2.2 新增子智能体角色

子智能体是配置数据，不需要写代码：在 **AI 助手 > 子智能体** 新建角色，指定说明书（绑定 Skill 或内联提示词）、可读材料和轮次上限即可。只有需要新增材料来源时才改代码——在 `src/types/subAgent.ts` 的 `SUB_AGENT_MATERIALS` 中扩展，并在 `subAgentMaterials.ts` 中实现装配。材料必须只读。

### 6.3 新增 Store Slice

1. 创建 `src/store/store.xxx.ts`，导出 `XxxSlice` 接口和 `createXxxSlice` 函数
2. 在 `src/store/useAppStore.ts` 中导入并聚合
3. 在 `AppState` 类型中添加 `& XxxSlice`
4. 审计新 Slice 的顶层导入，确认没有把低频重型运行时带入主窗口启动图
5. 运行类型检查、Store 定向测试和临时目录 sourcemap 构建

### 6.3.1 新增 IndexedDB object store

1. 在 `src/services/indexedDb/schema.ts` 中提升 `DB_VERSION`，声明 object store / index，并在 `openDB()` 的升级回调里保持旧数据可升级
2. 升级路径必须保证旧版本数据可读，不能依赖“新装用户”假设
3. 补对应的领域 CRUD，并在项目/会话删除路径上同步清理
4. 如果新数据属于项目域，同步扩展 `projectTransferService.ts` 的导出与导入
5. 运行 `npx vitest run tests/services/indexedDbService.test.ts`

### 6.3.2 新增原生 command

1. 在 `src-tauri/src/` 下实现，并在 `lib.rs` 的 `generate_handler!` 中注册
2. 只要参数里有路径，就必须先过 `path_policy.rs` 的调用方窗口校验和真实路径校验
3. 新增出网能力时复用 `assistant_web.rs` / `provider_docs.rs` 的校验模式，不要新开无校验的 HTTP 出口
4. 前端调用统一从 `fileService.ts` 或对应 service 暴露，不在组件里直接 `invoke`
5. 运行 `cargo check --lib` 与 `cargo test <module>::tests --lib`

### 6.4 更新 3D 导演台网页运行时版本

`lightweight-web` 导演台版本由 `scripts/director-desk-release.json` 固定，不跟随分支或 GitHub `latest`。先在 `Tenney95/3d-director-desk` 发布带 tag 的构建产物，再更新主项目。

1. 确认 Release 包含 `director-desk-vX.Y.Z.tar.gz` 和对应的 `.sha256` 文件。
2. 在 AI Canvas 仓库运行版本同步命令：

```bash
npm run director:update -- v0.3.2
```

3. 检查 `scripts/director-desk-release.json` 中的版本、URL、SHA-256 和体积差异。
4. 运行同步脚本、前端和 Rust 定向测试：

```bash
npx vitest run tests/scripts/updateDirectorDeskRelease.test.mjs
npx vitest run tests/services/directorDeskRuntimeService.test.ts
npx vitest run tests/services/directorDeskWindowService.test.ts
cargo test --manifest-path src-tauri/Cargo.toml director_desk_runtime::tests --lib
cargo check --manifest-path src-tauri/Cargo.toml --lib
```

5. 运行 `npm run build` 和 `npm run tauri build`，再发布新版 AI Canvas。

同步脚本会流式下载 Release 包，校验 SHA-256，检查 tar 路径、文件类型、入口和协议元数据，并原子更新清单。Rust 使用 `include_str!` 编译该清单，因此只替换远端 Release 不会更新已发布的 AI Canvas。

### 6.5 更新 Blender 内嵌运行资源

Blender 运行包与上一节的 `lightweight-web` Release 是两套独立版本。当前包版本是 `1.3.2`，由 `runtime-manifest.json` 的 `packageVersion` 和 `resources.rs` 的 `PACKAGE_VERSION` 共同锁定，不从 GitHub `latest` 下载。

1. 只修改 `src-tauri/resources/blender-runtime/v1/` 中受信的 manifest、Application Template、`startup.blend`、内置人物/许可证或固定 Job 脚本；不要加入任意用户 Python、自由 argv、环境变量或绝对输出路径。
2. 提升包版本，并同步 `runtime-manifest.json` 与 `resources.rs` 中的版本、资源 bytes 和 SHA-256。保留旧安装目录，不原地改写已安装版本。
3. 运行资源、Result Manifest 和前端投影定向检查：

```bash
cargo test --manifest-path src-tauri/Cargo.toml --no-default-features blender_runtime:: --lib
cargo check --manifest-path src-tauri/Cargo.toml --no-default-features --lib
npx vitest run tests/services/directorBlenderRuntimeService.test.ts tests/services/directorRuntimeRegistry.test.ts
npm run typecheck
```

4. 在 Windows Blender 5.2.1 LTS 真机验证普通启动不受影响，以及同一 `ai-director` 节点的打开、当前帧、参考视频、取消和保存返回。保存返回必须恰好提交当前帧与 `.blend` 两类新 artifact，并把当前帧更新为导演台最新预览；任一文件缺失、摘要不符或渲染失败都不得产生部分节点写回。

## 7. 常见问题

### Q: 前端开发模式下文件操作不可用？
A: 正常现象。Tauri 插件（fs、dialog）仅在 Tauri 桌面环境中可用。浏览器模式下自动降级为 File Input API + IndexedDB。同时凭据存储、项目导出/导入、MCP 控制桥和本地 ONNX 也不可用。

### Q: `npm run lint` 报 `scopeManager.addGlobals is not a function`？

A: 这是 ESLint 10 与解析器的既有兼容问题，不是本项目代码问题。遇到时记录该错误，改跑改动文件的定向检查：

```bash
npx eslint src/path/to/changed-file.ts
```

不要为了绕开它去改依赖版本，除非任务本身就是修工具链。

### Q: 浏览器模式下 API Key 每次都要重填？
A: 正常现象。凭据存储是 Rust 侧能力，浏览器模式没有它，API Key 只在当前会话内存中有效。桌面端凭据落在 `{appData}/secrets/credentials.json`。

### Q: 如何清除所有本地数据？
A: 打开浏览器 DevTools → Application → IndexedDB → 删除 `ai-canvas-db` 数据库。桌面端还需要删除项目数据目录和 `{appData}/secrets/`，前者可在 **设置 > 存储健康** 查看。

### Q: Tailwind class 不生效？
A: 检查 `tailwind.config.js` 的 `content` 配置是否包含对应文件路径。新增文件后可能需要重启开发服务器。

### Q: Tauri 构建失败？
A: 常见原因：
- Rust 工具链未安装或版本过低
- Visual Studio Build Tools 未安装
- `src-tauri/target/` 缓存损坏：删除后重试

### Q: Cargo 无法删除 `ai-canvas-tauri.exe`？

A: 旧的 Tauri 窗口仍占用 `src-tauri/target/debug/ai-canvas-tauri.exe`。先关闭应用窗口，并在任务管理器中结束残留的 `ai-canvas-tauri.exe`，再运行 `npm run tauri dev`。不要在进程仍运行时删除整个 `target` 目录。

### Q: 3D 导演台反复提示下载？

A: 应用只识别固定清单中的版本。检查 `scripts/director-desk-release.json`，确认本地目录包含匹配版本的 `index.html` 和 `director-desk-release.json`。开发环境可以在 **设置 > 存储健康** 删除资源后重新下载。

### Q: 3D 导演台下载失败？

A: 检查 GitHub Release 是否公开、产物名是否与清单一致，以及 `.sha256` 是否绑定同一文件。应用还会拒绝超过限制、包含不安全路径或协议元数据不匹配的归档。
